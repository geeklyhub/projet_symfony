{% extends 'base.html.twig' %}

{% block body %} {#Ici je vais afficher un seul article appelé#}
    <p>
        <article>
            <h2>{{ article.title }}</h2>
            <div class="metadata">Ecrit le {{ article.createdAt|date('d/m/Y') }} à {{ article.createdAt|date('H:i') }} dans la catégorie Informatique</div>
            <div class="content"><img src="{{ article.image }}" alt=""></div>


    <hr>
    {{ article.content|raw }}
    </article></p>
    <p>
{% endblock %}